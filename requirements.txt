# --- Recon module (in use now) ---
requests==2.32.3
beautifulsoup4==4.12.3
python-dotenv==1.0.1

# --- Scanning module (next phase) ---
# Same requests/bs4 above cover HTTP + response parsing for payload injection.
# Nothing extra needed yet — add here if you bring in a payload library.

# --- LLM triage module (Ollama-based) ---
ollama==0.3.3
pydantic==2.9.2          # for structured LLM output schema (verdict/confidence/explanation)

# --- Evaluation module ---
pandas==2.2.3             # for RQ3 rubric scoring, results aggregation
tabulate==0.9.0           # for readable rubric/report tables in terminal or notebooks

# --- Testing ---
pytest==8.3.3

# --- Security tooling (dev-time, not runtime) ---
pip-audit==2.7.3          # run before submission: pip-audit -r requirements.txt